# Write a program to accept distance in km and convert it into meters and
# centimeters both.

dist = int(input('enter the distance in km:  '))

#lets convert distance in km to meter and centimeter

meter = dist * 1000
print(meter)

centimeter = dist * 100000
print(centimeter)